{
  "name": "hookmark-mcp",
  "version": "0.1.0",
  "description": "MCP server for Hookmark — exposes the hook CLI as MCP tools for Claude Code",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "hookmark-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "keywords": [
    "mcp",
    "hookmark",
    "hook",
    "claude",
    "claude-code",
    "mcp-server",
    "macos",
    "productivity"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
